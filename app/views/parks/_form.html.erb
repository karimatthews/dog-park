<%= form_with(model: park, local: true) do |form| %>
  <% if park.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(park.errors.count, "error") %> prohibited this park from being saved:</h2>

      <ul>
      <% park.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :park_name %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :park_address %>
  </div>

  <div class="field">
    <%= form.label :offlead %>
    <%= form.check_box :offlead, id: :park_offlead %>
  </div>

  <div class="field">
    <%= form.label :fence %>
    <%= form.check_box :offlead, id: :park_fence %>
  </div>

  <div class="field">
    <%= form.label :bag %>
    <%= form.check_box :offlead, id: :park_bag %>
  </div>




  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
